"use client";
import { useState } from "react"; import { usePathname } from "next/navigation"; import { Sidebar } from "./sidebar"; import { Onboarding } from "@/components/onboarding";
const titles: Record<string, string> = { "/": "Dashboard", "/kanban": "Kanban", "/leads": "Leads", "/clientes": "Clientes", "/diagnosticos": "Diagnósticos", "/planejamentos": "Planejamentos", "/catalogo": "Catálogo", "/propostas": "Propostas", "/configuracoes": "Configurações", "/backup": "Backup e Dados", "/ajuda": "Ajuda" };
export function AppShell({ children }: { children: React.ReactNode }) { const [open, setOpen] = useState(false); const path = usePathname(); const presentation = path.startsWith("/apresentacao/"); if (presentation) return <><Onboarding />{children}</>; return <div className="app-shell"><Sidebar open={open} close={() => setOpen(false)} /><section className="workspace"><header><button className="menu" onClick={() => setOpen(true)} aria-label="Abrir menu">☰</button><div><p>WORKSPACE LOCAL</p><h1>{titles[path] ?? (path.startsWith("/clientes/") ? "Cliente" : "7Protect")}</h1></div><span className="local-badge">● Dados neste dispositivo</span></header><main>{children}</main></section><Onboarding /></div>; }
