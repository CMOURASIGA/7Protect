"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { useFoundation } from "@/components/foundation-provider"; import { PRODUCT_NAME, PRODUCT_SUBTITLE } from "@/lib/brand"; import { Icon } from "./icons";
const items = ["Dashboard", "Kanban", "Leads", "Clientes", "Diagnósticos", "Planejamentos", "Propostas", "Carteira", "Aegis", "Relatórios", "Configurações", "Backup e Dados"];
const routes: Record<string, string> = { Dashboard: "/", Kanban: "/kanban", Leads: "/leads", Clientes: "/clientes", "Configurações": "/configuracoes", "Backup e Dados": "/backup" };
export function Sidebar({ open, close }: { open: boolean; close: () => void }) { const pathname = usePathname(); const { settings } = useFoundation(); return <><button className={open ? "sidebar-backdrop visible" : "sidebar-backdrop"} aria-label="Fechar menu" onClick={close} /><aside className={open ? "sidebar-shell open" : "sidebar-shell"}><div className="brand-frame">{settings?.logoUrl ? <img src={settings.logoUrl} alt={settings.clientName} /> : <span>{settings?.clientName?.slice(0, 2).toUpperCase() ?? "7P"}</span>}</div><div className="product-lockup"><b>{PRODUCT_NAME}</b><span>{PRODUCT_SUBTITLE}</span></div><nav>{items.map((label) => { const href = routes[label]; const active = href ? pathname === href : false; return href ? <Link key={label} href={href} onClick={close} className={active ? "nav-item active" : "nav-item"}><Icon name={label} /><span>{label}</span></Link> : <div key={label} className="nav-item disabled"><Icon name={label} /><span>{label}</span><em>em breve</em></div>; })}</nav></aside></>; }
