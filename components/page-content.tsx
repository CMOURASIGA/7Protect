"use client";
import Link from "next/link"; import { useFoundation } from "@/components/foundation-provider";
export function Dashboard() { const { settings } = useFoundation(); const cards = [["Leads no período", "12"], ["Diagnósticos concluídos", "7"], ["Em cálculo", "4"], ["Aguardando cliente", "3"], ["Fechados no período", "5"], ["Capital protegido", "R$ 2,4 mi"]]; return <div className="page"><section className="hero"><div><p className="eyebrow">{settings?.clientName ?? "Sua corretora"}</p><h2>Visão operacional de proteção financeira</h2><p>Dados demonstrativos persistidos localmente. A fundação está pronta para os próximos módulos.</p></div><Link className="button primary" href="/configuracoes">Configurar marca</Link></section><div className="metrics">{cards.map(([label, value]) => <article className="card" key={label}><p>{label}</p><strong>{value}</strong><span>Dados demo</span></article>)}</div><section className="grid"><article className="card wide"><p className="eyebrow">FUNIL INICIAL</p><h3>Operação preparada para evoluir</h3><div className="funnel">{["Novo lead", "Formulário", "A calcular", "Apresentado", "Fechado"].map((x, i) => <div key={x}><b>{12 - i * 2}</b><span>{x}</span></div>)}</div></article><article className="card"><p className="eyebrow">PRÓXIMAS AÇÕES</p><h3>Reunião de apresentação</h3><p>Rafael Mendes · em dois dias</p></article></section></div> }
export function Placeholder({ title }: { title: string }) { return <div className="page"><section className="empty"><p className="eyebrow">ESTRUTURA PREPARADA</p><h2>{title}</h2><p>Este módulo será desenvolvido em uma SPEC posterior. A navegação e a base local-first já estão prontas.</p></section></div> }
