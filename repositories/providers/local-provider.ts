"use client";
import { db } from "@/repositories/local/database"; import { IndexedDbRepository } from "@/repositories/local/indexeddb-repository";
export const localProvider = { kind: "local" as const, tenants: new IndexedDbRepository(db.tenants), settings: new IndexedDbRepository(db.settings), leads: new IndexedDbRepository(db.leads), clients: new IndexedDbRepository(db.clients), tasks: new IndexedDbRepository(db.tasks), diagnostics: new IndexedDbRepository(db.diagnostics), planningCycles: new IndexedDbRepository(db.planningCycles), planningEvents: new IndexedDbRepository(db.planningEvents), brokerAnalyses: new IndexedDbRepository(db.brokerAnalyses), insurers: new IndexedDbRepository(db.insurers), insuranceProducts: new IndexedDbRepository(db.insuranceProducts), coverageTypes: new IndexedDbRepository(db.coverageTypes), proposalVersions: new IndexedDbRepository(db.proposalVersions), coverages: new IndexedDbRepository(db.coverages), closings: new IndexedDbRepository(db.closings), pipelineHistory: new IndexedDbRepository(db.pipelineHistory) };
export type RepositoryProvider = typeof localProvider;
