export type PlanningNumbers = { familyIncome: number; monthlyExpense: number; monthlyPremium: number; protectedCapital: number; estimatedNeed?: number };
export function calculatePlanning(numbers: PlanningNumbers) { const annualExpense = numbers.monthlyExpense * 12; const annualPremium = numbers.monthlyPremium * 12; return { familyIncome: numbers.familyIncome, annualExpense, annualPremium, protectedCapital: numbers.protectedCapital, incomeCommitment: numbers.familyIncome ? Number(((numbers.monthlyPremium / numbers.familyIncome) * 100).toFixed(2)) : 0, estimatedNeed: numbers.estimatedNeed ?? 0 }; }
